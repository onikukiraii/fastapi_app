{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q '\"command\".*git commit'; then cd \"$CLAUDE_PROJECT_DIR\" && mise run lint:all 2>&1 && mise run test 2>&1; fi",
            "timeout": 120000,
            "blocking": true
          }
        ]
      }
    ]
  }
}
